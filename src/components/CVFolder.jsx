import { motion } from 'framer-motion'
import CVPage from './CVPage'
export default function CVFolder({ page, setPage, pages, onReplay }) { const p=pages[page]; return <motion.div initial={{opacity:0,scale:.9}} animate={{opacity:1,scale:1}} className='absolute inset-0 grid place-items-center p-4'><div className='w-full max-w-3xl rounded-2xl bg-[#efe2c9] text-stone-900 p-6 shadow-2xl relative'><div className='absolute top-3 right-3 border-2 border-red-700 text-red-700 px-2 py-1 text-xs rotate-[-6deg]'>CONFIDENTIAL</div><CVPage p={p}/><div className='flex gap-2 mt-4'>{page>0&&<button className='btn' onClick={()=>setPage(page-1)}>Previous</button>}{page<pages.length-1?<button className='btn' onClick={()=>setPage(page+1)}>Next Page</button>:<><a href='mailto:volk.na.oxote@gmail.com' className='btn'>Send Email</a><button className='btn' onClick={onReplay}>Replay Story</button><button className='btn'>Download CV placeholder</button></>}</div></div></motion.div> }
